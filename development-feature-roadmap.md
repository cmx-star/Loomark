# Loomark 整体功能推进路线图

> 文档状态：执行规划稿 v1.0
>
> 编制日期：2026-08-24
>
> 规划基线：当前 `feat/large-file-windowed-gui` 分支
>
> 主要参考：`large-markdown-editor-technical-solution-and-roadmap.md`
>
> 用途：把总体技术方案转换为可连续实施、可逐批验收、可形成用户功能的开发顺序

## 1. 这份路线图解决什么问题

原技术方案给出了完整产品目标、架构边界和 P0～P6/M1～M3 阶段，但阶段跨度较大，不能直接回答以下执行问题：

- 下一次开发具体推进哪些模块。
- 一个模块完成后只是底层能力，还是已经形成用户可用功能。
- 需要累计完成几个模块，才能发布一个独立可验收版本。
- 当前 Qt Widgets 原型如何演进到方案中的大文件编辑、原生虚拟预览、工作区、AI 和移动端目标。
- 哪些技术路径可以直接推进，哪些涉及依赖、许可证或公共契约，必须先单独决策。

因此本文把开发过程拆成 **19 个推进批次、87 个模块**。每个批次都包含模块范围、完成后的用户能力、验收门槛和原方案引用。模块编号是依赖顺序，不代表必须一个模块对应一个 Git 提交。

## 2. 规划原则

### 2.1 “模块完成”和“功能完成”不是一回事

- **模块完成**：代码、测试和文档达到该模块的验收条件，但可能还没有形成完整用户工作流。
- **功能完成**：相关模块在同一真实场景中串通，成功路径、失败路径和恢复路径都已验证。
- **版本可发布**：功能完成后，再满足对应平台构建、性能、安全、许可证和安装包门禁。

本路线图用“功能门”标记第一次形成完整用户价值的位置。例如，完成磁盘空间查询并不等于“大文件安全保存”完成；还需要流式临时文件、原子替换、失败清理和回归测试共同通过。

### 2.2 不把原型实现误当最终架构

当前仓库已经具备 Qt Widgets GUI、`QPlainTextEdit` 编辑、`md4qt` 原生预览和核心文件 API。它们是有效的产品原型和验证基础，但原方案中的最终目标是：

- 桌面端使用 Scintilla 或经过验证的等价大文件后端作为正文事实源。
- UI 不长期持有完整大文件的第二份字符串副本。
- 预览使用轻量块模型和可视区域虚拟化。
- 搜索、解析、索引、保存和 AI 任务携带文档版本并可取消。

因此，当前的“大文件 2MiB 编辑窗口”只能作为 **P0 桥接验证**，用于证明分档、异步索引、局部读取和安全保存链路，不能直接宣布完成“300MB 完整桌面编辑”。

### 2.3 技术方案不等于依赖授权

原方案推荐 Qt/QML、原生 Scintilla、MD4C 和 SQLite，但当前项目规则要求新依赖或技术栈切换单独说明并取得批准。执行本路线图时遵循以下边界：

- 保留当前 Qt Widgets + `md4qt` 路径，直到替换方案完成技术验证并获批准。
- Scintilla、Qt Quick/QML、MD4C、SQLite、Quarkdown 或其他新依赖分别设置决策门。
- QScintilla 默认不采用；许可证决定未解决前不得作为便捷替代。
- 不因路线图包含某项技术就自动安装、链接或发布它。

### 2.4 每批必须闭环

每次推进至少包含：实现、定向测试、真实场景验证、失败说明和计划状态更新。不能用后续批次弥补当前批次的数据损坏、界面冻结或虚假可用状态。

## 3. 当前基线

截至 2026-08-24，仓库已经具备以下能力：

- C++20 core：文件检查、桌面文件档位、字节范围读取、字面量搜索、字节位置定位、预览块索引和原子写入。
- CLI：`inspect`、`index`、`search`、`locate`、`chunk`。
- Qt Widgets GUI：打开、编辑、预览、保存、另存为、重新加载、状态栏。
- 原生 Markdown 预览：`md4qt` AST 转 `QTextDocument`，支持基础块、表格、图片、公式降级和安全链接策略。
- 公式链路：QuickJS 内嵌 MathJax SVG 渲染，QtSvg 转为原生文档资源。
- 工程能力：CMake、CTest、macOS/Linux/Windows 打包、版本检查和本地日志。

尚未完成的关键断层：

- LARGE/EXTREME 文件在 GUI 中仍可能全文读入编辑器。
- 大文件预览还没有完整的后台任务、取消、版本校验和虚拟渲染链路。
- 没有 `DocumentSession`、统一后端契约、标签页、恢复日志和文件监视。
- 没有经过 300MB/512MB 实测的正式桌面大文件编辑后端。
- 目录、Diff、多视图、Git、导出、AI、RAG、插件和移动端尚未进入正式实现。

当前工作区已有 M02、M03 的未提交实现片段：后台预览线程和 GUI 窗口化状态声明属于 M02，`availableDiskBytes()` 及 core 测试属于 M03。它们仍在实施中，尚不能据此宣布后台预览、窗口化编辑或流式安全保存完成。

## 4. 总体依赖顺序

```text
文件安全与大文件桥接
  -> 正式文档后端与性能基线
  -> DocumentSession / Workspace
  -> Markdown 块索引与虚拟预览
  -> 目录 / Diff / 多视图
  -> 所见即所得与主题
  -> 文件监视 / Git / 高级会话
  -> 基础与高级导出
  -> AI 对话与上下文
  -> AI 修改 / RAG / 记忆
  -> 桌面稳定发布
  -> 插件生态
  -> 移动端
```

AI、导出、Git 和插件都依赖稳定的 `DocumentSnapshot`、版本号、字节范围和安全写入契约。不能在文档后端尚未稳定时各自读取 UI 文本并建立另一套事实源。

## 5. 分批推进计划

### 批次 1：大文件 GUI 桥接与保存安全基线

**模块 M01～M04，共 4 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M01 文件分档接入 | GUI 按 NORMAL/LARGE/EXTREME/REJECT 显示行为和限制；大文件关闭折行及高成本默认项 | 四个档位均有明确且一致的打开结果；REJECT 不读取正文 |
| M02 窗口读取与后台预览桥接 | LARGE/EXTREME 只装载受限字节窗口；预览由后台 `buildPreviewIndex()` 构建；旧结果不得覆盖新文档 | 打开 64～512MB 样本时主界面可响应；预览有截断状态 |
| M03 流式安全保存 | 磁盘空间预检；流式写临时文件；同步/关闭；原子替换；失败清理 | 磁盘不足、写入失败和替换失败均保留原文件；保存不拼接完整大文件到内存 |
| M04 大文件样本与性能记录 | 建立 64MB、300MB、512MB、超长行、未闭合围栏样本生成方式及测量记录 | 记录打开、预览、保存、峰值内存和 UI 响应；样本不提交 Git |

**功能门 F01：P0 大文件桥接原型。** 完成 4 个模块后，用户可以在现有 GUI 中安全识别和有限操作 512MB 以内文件，并能验证异步预览与安全保存链路。它仍不是最终的 300MB 完整编辑器。

**参考：** [R4]、[R8]、[R13.2]、[R14]、[R15]、[R16-P0]。

### 批次 2：正式桌面编辑后端决策

**模块 M05～M08，共 4 个模块，累计 8 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M05 `IDocumentBackend` 契约 | 定义 snapshot、read、locateLines、apply、search、版本和取消语义 | core 契约测试覆盖版本变化、范围错误和取消 |
| M06 Scintilla 直接集成原型 | 在隔离原型中验证 Qt 事件、输入法、选择、撤销、Loader 和跨平台构建 | macOS 实测；Windows/Linux CI 至少构建；记录许可证与包体 |
| M07 等价后端对照验证 | 用现有 Widgets 桥接方案对比 300MB 打开、编辑、搜索、保存和内存 | 形成可复核指标，不以主观流畅度下结论 |
| M08 技术决策记录 | 冻结桌面后端、移动后端和预览 UI 演进路径 | 用户批准依赖、许可证、回退方案和迁移范围后才进入批次 3 |

**功能门 G01：P0 架构退出门。** 完成 8 个模块后，不一定增加新用户功能，但必须能够回答“正式桌面编辑后端是否足以支撑 300MB 完整编辑”。失败则调整技术栈，不进入 P1。

**参考：** [R5]、[R6.1]、[R6.2]、[R16-P0]、[R18]。

### 批次 3：桌面大文件完整编辑

**模块 M09～M12，共 4 个模块，累计 12 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M09 正式桌面后端 | 把获批后端接入 `IDocumentBackend`，正文只保留一个事实源 | 编辑、撤销、选择和保存不通过第二份完整 UI 字符串 |
| M10 分块 Loader 与行索引 | 后台加载、稀疏行索引、BOM/换行/指纹和可取消进度 | 300MB 打开不冻结；取消后资源释放 |
| M11 大文件搜索替换 | 分批结果、结果上限、正则超时、大操作成本预估 | 300MB 搜索可取消；超过 32MB 修改先确认 |
| M12 大文件保存与撤销预算 | 后端快照流式保存、恢复点、撤销内存预算和外部指纹复核 | 300MB 编辑保存可靠；磁盘不足不破坏文件 |

**功能门 F02：300MB 桌面完整编辑。** 完成 12 个模块后，用户首次获得原方案承诺的桌面大文件完整编辑、搜索、撤销和安全保存闭环。

**参考：** [R6]、[R8.3]、[R8.4]、[R10.1]、[R13]、[R14.2]、[R16-P1]。

### 批次 4：DocumentSession 与基础工作区

**模块 M13～M16，共 4 个模块，累计 16 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M13 `DocumentSession` | 管理路径、后端、版本、脏状态、编码、换行、档位和任务取消域 | 切换文档不会串状态或接收迟到任务结果 |
| M14 标签页模型 | 打开、关闭、固定、排序、同路径去重和未保存标记 | 多标签切换不复制完整大文件正文 |
| M15 Workspace 与最近文件 | 工作区路径、文件树、最近文件和权限边界 | 大目录按需加载；不可访问路径有明确错误 |
| M16 命令与状态系统 | 统一打开、保存、搜索、视图切换命令及状态栏 | 命令可根据当前 session、档位和权限正确启停 |

**功能门 F03：可长期使用的多文档工作区骨架。** 完成 16 个模块后，用户可以在同一窗口管理多个文档，而不是只能操作一个全局编辑器状态。

**参考：** [R6]、[R7.1]、[R7.5]、[R16-P1]、[R20.3]。

### 批次 5：恢复、文件监视与 V0.1 闭环

**模块 M17～M20，共 4 个模块，累计 20 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M17 会话持久化 | 保存工作区、标签顺序、模式、光标和阅读位置，不保存完整正文 | 重启可恢复多标签状态；大文件正文不进入设置文件 |
| M18 恢复日志 | 记录未保存编辑操作或受控增量恢复信息 | 进程异常退出后可恢复或明确放弃，不静默覆盖磁盘文件 |
| M19 文件监视与冲突 | 外部修改、重命名、删除和网络盘回退策略 | 脏文件不自动重载；提供 Diff/保留/重载/另存为选择 |
| M20 V0.1 回归与三平台门禁 | 工作区、编辑、保存、恢复、安装包和许可证通知统一验收 | macOS/Windows/Linux 构建和新鲜进程冒烟通过 |

**功能门 F04：V0.1 Workspace / Desktop Preview。** 完成 20 个模块后，形成“工作区 + 多标签 + 大文件编辑 + 安全保存 + 恢复”的第一个可持续使用版本。

**参考：** [R13.3]、[R15]、[R16-P1]、[R17]、[R20.3]、[R20.4]、[R20.13]。

### 批次 6：统一 Markdown 块模型与增量索引

**模块 M21～M24，共 4 个模块，累计 24 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M21 版本化 `MarkdownBlock` | blockId、documentId、version、sourceRange、headingPath、hash 和 payload | 编辑后块身份与源码范围可追踪 |
| M22 解析适配层 | 把当前 `md4qt` 能力接入统一块模型；是否切换 MD4C 单独评审 | 上层不依赖具体解析器 AST 类型 |
| M23 安全检查点 | 记录围栏、列表、引用、脚注和 HTML 状态；支持向后收敛 | 跨块语义 fixture 通过；无法收敛时后台扩大扫描 |
| M24 索引任务调度 | 文档版本、取消、预算、优先级和迟到结果丢弃 | 连续编辑不会发生旧索引覆盖新内容 |

**功能门 G02：原生视图共享语义基础。** 完成 24 个模块后，目录、预览、定位、Diff 和 AI 可以开始复用同一块语义，而不是各自解析一遍正文。

**参考：** [R4]、[R6.2]、[R9.1]、[R9.2]、[R15.1]。

### 批次 7：虚拟预览、目录与源码定位

**模块 M25～M28，共 4 个模块，累计 28 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M25 虚拟预览模型 | 轻量块描述、可视块创建、预加载窗口和布局缓存 | 10 万块滚动不创建 10 万个可视对象 |
| M26 大块二次虚拟化 | 超长代码块、巨型表格、图片解码预算 | 极端块不会拖死主界面 |
| M27 目录模型 | 标题路径、源码范围、块 ID 和按档位延迟构建 | 围栏内伪标题不进入目录；点击可定位 |
| M28 预览到源码定位 | 阅读锚点、块内比例、预览点击和源码范围跳转 | 编辑器与预览在版本一致时准确互相定位 |

**功能门 F05：大文件原生阅读器核心。** 完成 28 个模块后，用户可以对大文件进行章节预览、虚拟滚动、目录导航和源码定位。

**参考：** [R7.3]、[R9.3]、[R10.2]、[R14.2]、[R16-P2]。

### 批次 8：多视图、同步与 Diff

**模块 M29～M33，共 5 个模块，累计 33 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M29 模式状态机 | CODE/PREVIEW/SPLIT/PARALLEL_PREVIEW/DIFF 与面板状态解耦 | 模式组合不产生冲突枚举和隐式正文副本 |
| M30 分屏同步 | 同文档源码范围锚点同步和回环抑制 | 用户滚动任一侧均无抖动和重复回传 |
| M31 并排预览 | 同文档双位置、异文档对照和主动窗格切换 | 两侧状态独立；大文件共享索引而非复制模型 |
| M32 语义同步策略 | SourceRange、HeadingPath、PairedAnchor、显式 Proportional | 低置信度停止跟随，不跳到无关章节 |
| M33 Diff 模型 | 磁盘基线、变更块、可视 hunk、版本失效和定位 | 不保留打开时完整原文；外部变化使旧 Diff 失效 |

**功能门 F06：Desktop Reader。** 完成 33 个模块后，交付原生阅读、分屏、并排对照、目录和 Diff 的完整阅读技术预览。

**参考：** [R7]、[R10.3]、[R16-P2]、[R17]、[R20.2]。

### 批次 9：所见即所得与主题

**模块 M34～M38，共 5 个模块，累计 38 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M34 可编辑块契约 | 标题、段落、列表、引用、任务项等块到源码 EditBatch 的映射 | 修改只作用于目标源码范围，版本冲突时拒绝 |
| M35 常用块直接编辑 | 标题、段落、列表、引用、任务项、链接和图片基础交互 | 往返源码不发生静默语义变化 |
| M36 表格与不支持块回退 | 表格基础编辑；扩展块保留原文并提供源码入口 | 未支持语法不会被重写或丢失 |
| M37 大文件章节级可视编辑 | LARGE/EXTREME 只开放当前章节或可视块 | UI 明确显示局部范围，不伪装全文已加载 |
| M38 四层主题 | 应用、编辑器、阅读和导出主题选择、预览、回退 | 主题损坏不影响文档内容；高对比模式可用 |

**功能门 F07：Visual Preview。** 完成 38 个模块后，用户可以在代码、阅读、分屏和所见即所得之间无损切换，并拥有可恢复主题。

**参考：** [R7]、[R16-P2V]、[R17]、[R20.2]、[R20.6]。

### 批次 10：Git 与高级工作区

**模块 M39～M43，共 5 个模块，累计 43 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M39 Git 只读状态 | 仓库识别、分支、文件状态、历史和行级 Diff | 大仓库按需查询；刷新不阻塞编辑 |
| M40 可逆 Git 写操作 | 暂存、取消暂存、提交和创建分支 | 仅用户显式触发；失败不改变文档内容 |
| M41 网络操作 | fetch/pull/push 状态、认证错误和冲突入口 | 不自动执行；网络中断可恢复 |
| M42 高风险操作保护 | 丢弃、回退、强制操作的二次确认和恢复提示 | 无明确确认不得执行不可逆操作 |
| M43 高级会话 | 最近关闭、标签固定、多窗格阅读位置和工作区恢复 | 重启后恢复视图但不持久化完整大文件 |

**功能门 F08：Git Workspace Preview。** 完成 43 个模块后，形成文件监视、Diff、Git 基础操作和高级会话的一体化工作区。

**参考：** [R16-P2G]、[R17]、[R18]、[R20.3]、[R20.4]、[R20.5]。

### 批次 11：基础导出

**模块 M44～M47，共 4 个模块，累计 47 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M44 文档配置 | Front Matter / `document.yml`、未知字段保留和解析诊断 | 配置失败回退普通 Markdown，不改写未知字段 |
| M45 `ExportManifest` 与任务模型 | snapshot、manifest、planner、backend、取消和事件流 | 导出不从 UI 拼接完整字符串 |
| M46 GFM/TXT 流式导出 | 编码、换行、选中章节和原子输出 | 大文件可流式导出；失败不覆盖旧输出 |
| M47 安全静态 HTML | 原生生成文件、资源预算、链接和原始 HTML 安全策略 | 不依赖 WebView；不执行脚本和远程内容 |

**功能门 F09：基础发布能力。** 完成 47 个模块后，用户可以可靠导出 GFM、TXT 和安全静态 HTML。

**参考：** [R13]、[R19.2]、[R19.3]、[R19.7]、[R20.7]。

### 批次 12：高级文档生产与 Export Center

**模块 M48～M52，共 5 个模块，累计 52 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M48 Export Center | 目标、主题、页面、目录、资源、进度、日志和取消 | 可查看阶段、错误并重新运行 |
| M49 PDF 原生/隔离后端 | 页面、字体、目录、临时输出和验证 | 失败不覆盖旧 PDF；大文件按档位限制 |
| M50 DOCX 后端 | 标题、段落、列表、表格、图片和样式映射 | 不支持语义产生可定位告警，不静默丢失 |
| M51 文章/书籍/演示/站点预设 | 文档类型、资源复制、输出目录和构建清单 | 相同输入可追踪重建 |
| M52 可选外部编译器 | Quarkdown/Pandoc/Typst 适配门、隔离、权限和许可证 | 未批准或不可用时保持禁用，不影响基础导出 |

**功能门 F10：Publishing Preview。** 完成 52 个模块后，交付原生 Export Center、主题化 PDF/DOCX 和高级文档生产预设。

**参考：** [R16-P2E]、[R17]、[R19]、[R20.7]、[R20.8]。

### 批次 13：AI 对话、上下文与来源

**模块 M53～M57，共 5 个模块，累计 57 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M53 Provider 抽象 | OpenAI-compatible 协议、能力查询、流式事件、错误和取消 | 应用层不依赖供应商私有字段 |
| M54 原生 AI 面板 | 对话、停止、重试、状态和原生 Markdown 消息渲染 | 不使用 WebView；取消后无迟到覆盖 |
| M55 上下文装箱 | 选区、前后文、当前章节、指定文件和权限摘要 | 请求前可见来源与预计用量；未授权内容不发送 |
| M56 大文件分层摘要 | 块摘要、章节摘要、文件摘要和版本化缓存 | 不把整篇大文件直接发送给 Provider |
| M57 密钥与隐私 | 平台安全存储、日志脱敏、超时和网络错误 | 日志、fixture 和崩溃信息不含密钥或正文 |

**功能门 F11：AI Preview。** 完成 57 个模块后，用户可以安全地进行流式对话、选区解释、章节总结和大文件分层摘要。

**参考：** [R4]、[R11.1]、[R11.2]、[R11.3]、[R13.4]、[R13.5]、[R16-P3]。

### 批次 14：AI 修改闭环

**模块 M58～M61，共 4 个模块，累计 61 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M58 `EditProposal` | baseVersion、baseFingerprint、EditBatch、说明和来源 | 提案不直接触碰正文 |
| M59 AI Diff 确认 | 复用文档 Diff 模型展示范围和影响 | 用户逐项接受/拒绝；默认不写入 |
| M60 版本冲突与重算 | 文档变化后拒绝旧提案，可显式重新生成 | 过期补丁不能套用到新内容 |
| M61 应用与撤销 | 后端 apply、单次撤销组、恢复日志和保存前确认 | 应用失败保持原文；成功后可撤销 |

**功能门 F12：安全 AI 写作。** 完成 61 个模块后，AI 可以提出并应用修改，但所有写入都经过版本校验、原生 Diff 和用户确认。

**参考：** [R11.4]、[R11.5]、[R16-P4]、[R20.10]、[R20.12]。

### 批次 15：RAG、来源、记忆与受控 Agent

**模块 M62～M67，共 6 个模块，累计 67 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M62 语义分块 | 复用 MarkdownBlock 和标题路径，稳定 chunkId | 局部编辑只使受影响块失效 |
| M63 Embedding 队列 | 版本、取消、重试、配额和后台优先级 | 不阻塞编辑、预览或保存 |
| M64 SQLite 元数据 | schema、迁移、文档指纹和索引版本 | 旧索引迁移或重建可验证 |
| M65 检索与来源跳转 | 查询、过滤、得分、sourceRange 和预览定位 | 每条引用可跳回原文 |
| M66 长期记忆候选 | 建议、来源、确认、编辑、删除和作用域 | 模型不能静默写入长期记忆 |
| M67 联网搜索与只读 Agent | 工具权限、超时、结果预算、审计和默认只读 | 未授权工具不可调用；写操作仍走提案协议 |

**功能门 F13：AI 知识工作区。** 完成 67 个模块后，形成带来源的知识库问答、长期记忆候选和受控只读 Agent。

**参考：** [R11.5]、[R12]、[R13.5]、[R16-P4]、[R18]。

### 批次 16：桌面稳定性与正式发布

**模块 M68～M72，共 5 个模块，累计 72 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M68 性能门禁 | 300MB 打开、滚动、搜索、预览、保存、Diff 和 AI 基线 | 性能回归有固定设备、样本和阈值 |
| M69 崩溃与恢复 | 崩溃诊断、恢复日志校验、安全模式和损坏配置回退 | 崩溃后不损坏原文，恢复过程可解释 |
| M70 升级迁移 | 设置、会话、数据库和索引版本迁移 | 旧版本数据可升级或明确回退 |
| M71 三平台发布 | 安装包、签名、公证、自动更新和第三方通知 | Windows/macOS/Linux 新鲜安装验证通过 |
| M72 可访问性与真实回归 | 键盘导航、屏幕阅读、高对比、真实大文件和长期运行 | 发布清单全部通过，残余风险有记录 |

**功能门 F14：Desktop 1.0。** 完成 72 个模块后，桌面端具备完整文档工作流、AI 修改与知识能力，以及可发布的三平台稳定性。

**参考：** [R14]、[R15]、[R16-P5]、[R17]、[R20.11]、[R20.13]。

### 批次 17：插件能力

**模块 M73～M77，共 5 个模块，累计 77 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M73 插件清单与兼容性 | id、版本、平台、宿主范围和签名元数据 | 不兼容插件不能加载 |
| M74 权限模型 | 文件、工作区、网络、AI、导出和写入权限 | 安装/启用前展示权限；默认最小授权 |
| M75 扩展点 | 命令、主题、语法、导出器、AI Provider 和低风险动作 | 禁用插件后核心功能保持完整 |
| M76 隔离与安全模式 | 故障隔离、超时、启动禁用和诊断 | 故障插件不阻止打开或保存文档 |
| M77 管理界面与官方示例 | 安装、启停、更新、按工作区启用和示例插件 | 权限变化需重新确认；升级失败可回退 |

**功能门 F15：Extensions Preview。** 完成 77 个模块后，开放受控插件扩展，同时保持核心编辑、安全保存和 AI 权限独立。

**参考：** [R16-P6]、[R17]、[R18]、[R20.1]、[R20.9]。

### 批次 18：移动共享核心与阅读编辑

**模块 M78～M82，共 5 个模块，累计 82 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M78 移动共享 core | Android/iOS 编译、线程、网络、SQLite 和取消契约 | 真机运行，不只验证模拟器 |
| M79 文件与安全存储 | 文件选择器、沙箱授权、生命周期和密钥存储 | 返回前台后权限和文件指纹重新确认 |
| M80 响应式状态与布局 | PHONE/TABLET、抽屉、模式切换和软键盘行为 | 键盘不遮挡编辑范围；手机不强行三栏 |
| M81 100MB 虚拟阅读 | 分块索引、目录、搜索、虚拟预览和资源预算 | 100MB 阅读不发生明显内存杀死 |
| M82 20MB 分段编辑 | PagedDocumentBackend、章节/行窗口、触摸选择和撤销 | 20MB 完整文件可修改；编辑器只装载当前窗口 |

**功能门 F16：Mobile Preview。** 完成 82 个模块后，手机和平板具备 100MB 阅读、20MB 分段编辑、目录、搜索和基础会话能力。

**参考：** [R6.1]、[R7.6]、[R8.2]、[R16-M1]、[R16-M2]、[R17]、[R20.11]。

### 批次 19：移动 AI、导出与发布

**模块 M83～M87，共 5 个模块，累计 87 个模块。**

| 模块 | 推进内容 | 模块验收 |
| --- | --- | --- |
| M83 移动 AI | 选区、当前章节、流式回答、停止和修改确认 | 权限与桌面共享；上下文按移动预算缩减 |
| M84 移动基础导出 | GFM、TXT、安全 HTML、系统分享和小文件 PDF | 高成本目标明确禁用或降级 |
| M85 前后台与任务恢复 | 网络、索引和 AI 任务的暂停、取消、恢复 | 应用切后台后无迟到覆盖或资源泄漏 |
| M86 移动资源门禁 | 内存、电量、流量、索引配额和异常样本 | 真实低配设备达到档位目标 |
| M87 商店发布 | 权限说明、隐私、签名、升级和真机回归 | Android/iOS 发布清单通过 |

**功能门 F17：Mobile 1.0。** 完成 87 个模块后，移动端具备阅读、分段编辑、基础 AI、基础导出和正式发布能力。

**参考：** [R12.3]、[R16-M3]、[R17]、[R20.11]、[R20.13]。

## 6. 功能完成节点总表

| 累计模块 | 完成节点 | 用户得到的完整能力 |
| ---: | --- | --- |
| 4 | F01 P0 大文件桥接原型 | GUI 分档、窗口读取、后台预览和保存安全验证 |
| 8 | G01 P0 架构退出门 | 正式桌面后端有实测结论，可以决定是否进入 P1 |
| 12 | F02 300MB 桌面完整编辑 | 大文件完整编辑、搜索、撤销和安全保存 |
| 16 | F03 多文档工作区骨架 | DocumentSession、标签页、工作区和统一命令 |
| 20 | F04 V0.1 Workspace | 恢复、文件监视和三平台 Desktop Preview |
| 24 | G02 统一块语义基础 | 目录、预览、Diff、AI 共用版本化 Markdown 块 |
| 28 | F05 大文件原生阅读器 | 虚拟预览、目录和源码定位 |
| 33 | F06 Desktop Reader | 分屏、并排同步、Diff 和完整阅读工作流 |
| 38 | F07 Visual Preview | 所见即所得、无损模式切换和主题 |
| 43 | F08 Git Workspace Preview | Git、冲突流程、文件监视和高级会话 |
| 47 | F09 基础发布能力 | GFM、TXT、安全 HTML 流式导出 |
| 52 | F10 Publishing Preview | Export Center、PDF、DOCX 和高级文档生产 |
| 57 | F11 AI Preview | 原生 AI 对话、选区/章节上下文和分层摘要 |
| 61 | F12 安全 AI 写作 | AI Diff、版本校验、确认应用和撤销 |
| 67 | F13 AI 知识工作区 | RAG、来源、记忆候选和受控 Agent |
| 72 | F14 Desktop 1.0 | 三平台稳定发布和完整桌面工作流 |
| 77 | F15 Extensions Preview | 有权限与隔离的插件能力 |
| 82 | F16 Mobile Preview | 100MB 阅读和 20MB 分段编辑 |
| 87 | F17 Mobile 1.0 | 移动 AI、基础导出和商店发布 |

## 7. 推荐的近期推进顺序

当前只应把资源投入到批次 1，完成后再进入批次 2。近期顺序如下：

1. 完成 M03 当前已开始的磁盘空间查询，但把“磁盘预检”和“安全保存完成”分开验收。
2. 完成 M01 文件档位和降级 UI，确保打开前就决定行为。
3. 完成 M02 大文件局部读取与版本化后台预览；先证明 UI 不冻结。
4. 回到 M03，补齐流式临时文件、原子替换、失败清理和跨平台测试。
5. 完成 M04 固定样本与性能记录，给 P0 后端决策提供数据。
6. 进入批次 2 前，单独提交 Scintilla/等价后端的依赖、许可证、风险和验证方案，取得批准。

不建议在 F02 之前推进 AI、Git 写操作、WYSIWYG 或高级导出，因为它们都会放大尚未稳定的文档版本、范围定位和保存契约问题。

## 8. 每批统一验收模板

每个批次完成时至少记录：

- **行为证据**：对应用户场景实际执行结果，不只说明代码结构存在。
- **自动检查**：受影响单测、契约测试、构建和 `git diff --check`。
- **性能证据**：涉及大文件时记录样本大小、设备、耗时、峰值内存和可取消性。
- **失败路径**：磁盘不足、权限失败、取消、版本冲突、外部变化、网络中断等相关场景。
- **跨平台状态**：明确哪些平台已构建、已运行、仅 CI 验证或尚未验证。
- **依赖与许可**：新增依赖的版本、许可证、替代方案和分发通知。
- **范围核对**：没有夹带后续模块或无关重构；无法验证项和残余风险明确记录。

## 9. 决策门

以下事项不能由实施阶段默认决定：

| 决策门 | 最晚决策时间 | 必须提供的证据 |
| --- | --- | --- |
| D01 桌面编辑后端 | M06 前批准验证；M08 冻结结论 | 300MB/512MB 指标、Qt 集成成本、许可证、包体和回退方案 |
| D02 解析器长期路径 | M22 前 | 当前 `md4qt` 缺口、MD4C 收益、迁移成本、跨块语义测试和许可 |
| D03 QML/虚拟预览 | M25 前 | Qt Widgets/QAbstractItemView 与 QML ListView 原型对比、移动复用价值和依赖影响 |
| D04 SQLite | M64 前 | schema、迁移、并发、包体和无数据库替代方案 |
| D05 外部导出器 | M52 前 | 许可证、隔离、权限、大文件基准和未安装时的降级行为 |
| D06 AI Provider 与密钥 | M53 前 | 首批协议范围、存储方式、隐私、日志和网络失败契约 |
| D07 插件边界 | M73 前 | 扩展点、权限分类、隔离能力、签名和兼容策略 |
| D08 移动首发范围 | M78 前 | iOS/Android 顺序、真机、文件权限和性能设备 |

## 10. 参考索引

### 10.1 主要方案来源

以下编号均指根目录 `large-markdown-editor-technical-solution-and-roadmap.md`。带小节后缀的引用直接对应原文小节，例如 `[R8.3]` 表示 §8.3；`[R16-P2V]` 表示 §16 的 P2V 阶段：

| 编号 | 参考章节 | 本路线图采用的内容 |
| --- | --- | --- |
| R2 | §2、§2.1、§2.2 | 产品定位、桌面/移动文件目标和首版排除项 |
| R4 | §4 架构红线 | 无 WebView、无大文件第二副本、后台线程、取消、版本和文件安全 |
| R5 | §5 技术栈 | Qt/QML、Scintilla、解析器、SQLite 候选及依赖门槛 |
| R6 | §6、§6.1、§6.2 | 分层架构、`IDocumentBackend`、snapshot 和线程模型 |
| R7 | §7 | 模式状态、阅读锚点、分屏和并排同步 |
| R8 | §8 | 桌面/移动文件档位、加载和高成本操作保护 |
| R9 | §9 | Markdown 块模型、增量索引、虚拟渲染和特殊语法 |
| R10 | §10 | 搜索、目录和 Diff 契约 |
| R11 | §11 | AI 分层、Provider、上下文、修改协议、Agent 和界面 |
| R12 | §12 | RAG 分块、索引、移动策略和长期记忆 |
| R13 | §13 | 文件访问、保存、恢复、密钥和网络安全 |
| R14 | §14 | 参考设备、性能指标和可观测性 |
| R15 | §15 | 固定样本、测试层级和发布门禁 |
| R16-P0～P6 | §16 | 桌面各开发阶段的范围和验收 |
| R16-M1～M3 | §16 | 移动共享核心、阅读编辑、AI 和发布范围 |
| R17 | §17 | Desktop/Mobile 各发布切片和独立验收原则 |
| R18 | §18 | 技术、许可、安全、性能和范围风险 |
| R19 | §19 | 文档配置、导出架构、外部编译器、脚本和 Export Center |
| R20 | §20 | 完整产品功能分级、工作区、Git、主题、导出、插件、AI 和发布路线 |

### 10.2 当前仓库事实来源

| 来源 | 用途 |
| --- | --- |
| `README.md` | 当前 GUI、原生预览、构建、打包、日志和更新能力 |
| `CMakeLists.txt` | 当前 Qt Widgets、Qt Network/Svg、`md4qt`、QuickJS、测试和打包依赖 |
| `src/core/document_file.*` | 文件检查、字节读取、搜索、定位、原子写入和当前磁盘空间预检 |
| `src/core/file_tier.*` | NORMAL/LARGE/EXTREME/REJECT 档位阈值 |
| `src/core/markdown_index.*` | 当前 Heading/Paragraph/CodeFence 预览索引原型 |
| `src/gui/main_window.*` | 当前单文档 Qt Widgets 编辑、预览和保存状态 |
| `src/gui/markdown_document_renderer.*` | 当前 `md4qt` 到 `QTextDocument` 的原生 Markdown 渲染路径 |
| `.trellis/tasks/08-24-large_file_windowed_gui/` | 当前大文件 GUI 桥接任务的 PRD、设计和实施切片 |

### 10.3 来源使用说明

- 原方案的 §22 “当前工作区状态”描述的是 2026-08-21 生成方案时的旧状态，当前仓库已经初始化并拥有可运行代码，因此本文不沿用该节的状态结论。
- 原方案推荐的技术栈是目标与候选，不覆盖当前项目的依赖审批规则。
- 当前仓库事实只用于确定起点，不降低原方案对大文件、线程、版本、安全和跨平台的验收要求。
- 当代码、测试或已批准决策与本文不一致时，应先更新本文的对应批次和参考依据，再扩大实施范围。
