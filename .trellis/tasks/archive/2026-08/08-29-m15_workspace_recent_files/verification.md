# M15 Workspace 与最近文件 验证记录

> 日期：2026-08-29
> 验收口径（用户调整）：编译无错 + ctest 通过 + 打包产物生成即为通过；深度验证后补。

| 项 | 结果 |
| --- | --- |
| 编译（0 错误 0 警告，上游 1 处除外） | ✅ |
| ctest 10/10（含命令启停矩阵、最近文件去重、标签/会话/装载回归） | ✅ |
| git diff --check | ✅ |
| 打包目标可构建（cpack/打包链路已在 CI 覆盖，本地以 GUI 产物生成为准） | ✅ |

实现：M15 最近文件（QSettings 去重上限 10、缺失明确报错）+ 工作区面板
（QFileSystemModel 懒加载、不可读目录明确报错、点击打开）；
M16 命令注册表（open/save/save-as/reload/find/workspace 按会话/档位/装载状态启停）。
