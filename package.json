{
  "name": "markdown-qt",
  "private": true,
  "scripts": {
    "build:mathjax-bundle": "esbuild tools/quickjax_renderer/index.js --bundle --minify --platform=browser --format=iife --outfile=build/mathjax-runtime/mathjax_bundle.js",
    "verify:mathjax": "npm run build:mathjax-bundle && node -e \"const fs=require('fs'),vm=require('vm');const ctx={};vm.createContext(ctx);vm.runInContext(fs.readFileSync('build/mathjax-runtime/mathjax_bundle.js','utf8'),ctx);const svg=ctx.renderInline('x^2 + y^2');if(!svg.startsWith('<svg')||!svg.includes('</svg>'))process.exit(1);console.log('mathjax-bundle-ok',svg.length);\""
  },
  "dependencies": {
    "mathjax-full": "4.0.0-beta.7"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
